application: mindale-com

dispatch:
# Default module serves the typical web resources and all static resources.
- url: "/*"
module: default

# Send all backend traffic to the backend.
- url: "/backend/*"
module: backend
