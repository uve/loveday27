application: mindale-com
module: backend
version: v1
runtime: go
api_version: go1
threadsafe: true

#instance_class: F1

#automatic_scaling:
#  min_idle_instances: 1
#  max_pending_latency: 1s

handlers:

- url: /backend/.*
  script: _go_app